# 下一阶段任务计划

> 基于 `docs/PROGRESS.md`（2026-08-13）和阶段五实施计划整理。本文只描述从当前状态向前推进的任务，不重复历史完成项。

## 当前判断

- mock 纵向链路、定时调度、持久化去重、dry-run 推送和五层演示均已完成。
- 阶段五首批 3 个真实平台 parser 与受限 smoke 已完成；技术核验已登记，服务条款仍待业务/法务最终复核。
- 阶段二还欠一次真实 P2 资料命中补测和规则终版校正，但不阻塞阶段五工程收尾。
- 当前应避免继续扩展 mock 功能；优先完成可验收性、配置安全和真实平台接入准备。
- 当前本地验证已完成：`dotnet build` 0 警告/0 错误，`dotnet test` 366 通过/7 跳过，API 健康检查返回 `ready`。

## 当前关键事项

| 编号 | 状态 | 事项 | 责任方 | 登记日期 |
|---|---|---|---|---|
| `P5-COMPLIANCE-001` | 技术完成/法务待复核 | 首批 3 个真实平台技术核验与允许采集范围登记 | 开发、业务负责人、法务 | 2026-08-14 |

- **完成条件:** 三个平台的名称、官方域名、公开入口、`robots.txt` 结论、服务条款、频率限制、禁止项、确认人和确认日期全部登记在 [`docs/ops/bidding-collection-compliance.md`](ops/bidding-collection-compliance.md)。
- **并行策略:** 平台公开性与合规核验、平台 1 parser fixture、平台 2/3 接入模板、生产化配置审计和本地回归并发推进；真实 smoke 在对应平台的公开性与访问边界核验完成后立即执行。

## 下一阶段：真实平台并行核验与生产化准备

本阶段允许访问真实平台的公开入口，但不绕过登录、验证码或访问控制；继续保持不重新导入 Dify YAML、不操作用户正在使用的浏览器页面的约束。

| 编号 | 任务 | 依赖 | 完成标准 | 状态 |
|---|---|---|---|---|
| `P5-R1` | 本地构建与全量回归 | 无 | `dotnet build` 0 警告/0 错误；`dotnet test` 0 失败 | 已完成（2026-08-13） |
| `P5-R2` | API/Dify 调用边界复核 | API 已运行 | `/health` 返回 `200 ready`；鉴权失败、传输失败和业务失败边界保持可识别 | 已完成（2026-08-13） |
| `P5-R3` | 生产化配置差距审计 | 无 | 明确持久化台账、计划源、凭据注入、通知开关和监控告警的上线前补齐项 | 已完成（差距已登记） |
| `P5-R4` | Dify Workflow 文档冻结 | v2 已发布 | 保留已验证版本和四条回归证据；不因本地 YAML 解析工具缺失而重复导入 | 已完成 |
| `P5-R5` | 首批平台合规确认 | 无 | 三个平台的技术核验完成；业务/法务完成服务条款复核 | 技术完成/法务待复核 |
| `P5-R6` | 首批 3 个 parser 与真实 smoke | 平台公开入口核验 | 只采集公开页面；robots、限速、无 PII 和解析失败隔离回归通过 | 已完成（2026-08-14） |
| `P5-R7` | 持久化计划源与回滚 | 无 | 受控 JSON 热加载、严格校验、last-known-good、内容无关审计与轮转均有测试 | 已完成（2026-08-14） |
| `P5-R8` | 真实采集启动配置校验 | P5-R7 | 未知平台、不安全限速、相对/缺失持久化目录在启动时失败 | 已完成（2026-08-14） |
| `P5-R9` | 钉钉 Webhook 受控真实投递 | 用户创建测试群自定义机器人 | 先完成 URL 日志脱敏和钉钉 `errcode` 校验，再以隔离计划/台账完成 dry-run 与单条真实投递，最后恢复安全开关 | 下次继续 |

### 生产化差距

- `Bidding:LedgerRoot` 未配置时仍会退回内存台账；启用真实平台时启动校验已强制要求绝对路径，上线部署仍需验证目录 ACL、可写性及重启后公告去重和 `(planId, date)` 幂等。
- `IScheduledCollectionPlanSource` 已支持 `Bidding:PlanRoot` 下的受控 JSON 热加载、严格校验、last-known-good 回滚和 1 MiB 双文件审计轮转；默认未配置时仍安全地调度零个计划。生产约束为单实例写入同一 `PlanRoot`。
- `appsettings.Development.json` 中的 `demo-key-2026` 仅是本地演示值，不能复用为生产凭据；生产 API key、Webhook URL、SMTP 密码必须由环境变量或密钥库注入。
- 通知默认 `Enabled=false`、`DryRun=true` 的安全默认保持不变；真实投递仍需人工确认收件人后单次受控 smoke。
- Webhook 平台已选定为钉钉；用户下次先在专用测试群创建“自定义机器人”，安全模式选择关键词 `招投标通知`。完整 Webhook URL 不进入聊天、文档或仓库，只通过运行时环境变量/密钥库注入。

## 执行顺序

| 优先级 | 任务 | 依赖 | 完成标准 | 状态 |
|---|---|---|---|---|
| P0 | 首批 3 个公开平台核验与允许采集范围登记 | 无 | 每个平台记录入口、robots、频率与禁止项；服务条款完成复核 | 技术完成/法务待复核 |
| P0 | 优化并重新导入招标采集 Dify Workflow | 无 | 密钥不入库；可指定计划/日期；HTTP、JSON、业务失败和重复执行均可识别 | 已完成（v2 发布，四条回归通过） |
| P1 | 接入第 1 个真实平台 parser | 平台 1 公开入口核验 | 小样本采集成功；解析失败隔离；无原始 HTML 和敏感字段落库 | 已完成 |
| P1 | 复制接入平台 2、3 | 平台 1 接口模板通过 | 三个平台均通过限速 smoke，单平台故障不拖垮整体 | 已完成 |
| P1 | 真实 SMTP 或群 Webhook dry-run/受控 smoke | 收件人与目标地址人工确认 | 先 dry-run 对照，再单次真实投递；日志不含凭据和完整收件人 | 待开始 |
| P1 | 阶段五验收回归 | 真实 parser 与投递 smoke | `dotnet test` 全绿；真实端到端链路通过；合规台账完整 | 采集侧完成，待真实投递 |
| P2 | 真实 P2 资料命中补测与收录规则终版 | 一份获批 P2 资料 | R2 正向命中，迁移台账和收录规则完成校正 | 等待资料 |
| P2 | 生产化评估 | 验收回归完成 | 计划源持久化已完成；继续明确队列恢复、监控告警和容量基线的实施范围 | 进行中 |

## 最近一个工作批次

1. 已将 `docs/demo/bidding-collection-workflow.yml` 原地导入 Dify 并发布 `bidding-workflow-v2`（published workflow ID：`93d84226-e997-44d6-988b-4aad8dc36bae`）。
2. 首批接入调整为江苏省招标投标公共服务平台、全国公共资源交易平台和江苏政府采购网；中国政府采购网因 403、招标投标公共服务平台因 `Disallow: /` 未接入。
3. 三个平台 parser、离线 fixture、显式启用配置和受限真实 smoke 均已完成；真实 smoke 为 3 通过、0 失败。
4. 默认全量回归为 366 通过、7 跳过、0 失败；7 个跳过项为 4 个 FFmpeg 环境 smoke 和 3 个需 `MI_SMOKE_BIDDING=1` 的真实平台 smoke。
5. 已新增受控文件计划源：固定文件名、严格 JSON、最多 100 个计划/1 MiB、运行时重载、last-known-good 回滚和不含业务内容的有界审计。
6. 已增加真实采集配置启动校验：仅允许 3 个已接入平台，单平台间隔至少 1 秒，全局 QPS 为 1~5，启用真实平台时 `LedgerRoot`/`PlanRoot` 必须为绝对路径。
7. 真实通知目标已选择钉钉群自定义机器人，下次从 URL 日志脱敏、钉钉业务响应校验和机器人创建后的安全配置继续；本批次未创建机器人、未保存 Webhook URL、未产生真实投递。

## 回归证据

- API 健康：`GET http://127.0.0.1:5294/health` 返回 `{"status":"ready"}`。
- 首次成功：`status=success`、`plansExecuted=1`、`totalNoticesCollected=2`、`skippedCount=0`。
- 同日重复：`status=success`、`skippedCount=1`、计划 `outcome=skipped`、`error=already_completed`，不会再次推送。
- 错误密钥：`POST /api/bidding/collect` 返回 HTTP 401。
- API 不可达：连接未监听端口时识别为传输失败；Dify HTTP 节点的连接异常仍属于节点级失败，不伪装成业务成功。

## 暂不进入关键路径

- IMA 自动同步、真实视频号采集、TTS 商业音色。
- M4-05 TOCTOU/进程树加固和 M4-07 全量持久化队列。
- 高并发采集；首批平台保持串行、低频、小样本。
